import 'package:flutter/material.dart';

class play extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text('asdasd'),),
      body: Center(
        child: Text('clicked the play button'),
      ),
    );
  }

}

class delet extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text('asdasd'),),
      body: Center(
        child: Text('clicked the delete button'),
      ),
    );
  }

}